# WHAPI Cloud / WhatsApp integration
#
# Base API host from the official docs:
# - https://whapi.readme.io/reference/sendmessagetext
# - https://whapi.readme.io/reference/sendmessageimage
# - https://whapi.readme.io/reference/sendmessagedocument
# - https://whapi.readme.io/reference/uploadmedia
WHAPI_BASE_URL=https://gate.whapi.cloud

# Your channel token / API key (used as Bearer token by default)
WHAPI_TOKEN=PUT_YOUR_TOKEN_HERE

# If your account/setup uses token in query string instead of Authorization header:
WHAPI_TOKEN_IN_QUERY=false

# For upload endpoint (/media), WHAPI may expect either "media" or "file" as the multipart form field.
# If upload fails, try switching this.
WHAPI_UPLOAD_FIELD=media

# Webhook listener settings (for local testing)
WHAPI_WEBHOOK_HOST=0.0.0.0
WHAPI_WEBHOOK_PORT=8080
WHAPI_WEBHOOK_PATH=/webhook


